<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.finance.mapper.AccountMapper">

    <resultMap id="BaseResultMap" type="com.ims.finance.entity.Account">
        <id column="id" property="id"/>
        <result column="account_no" property="accountNo"/>
        <result column="account_name" property="accountName"/>
        <result column="account_type" property="accountType"/>
        <result column="bank_name" property="bankName"/>
        <result column="bank_account" property="bankAccount"/>
        <result column="balance" property="balance"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, account_no, account_name, account_type, bank_name, bank_account, 
        balance, status, remark, create_time, update_time, deleted
    </sql>

    <select id="selectByAccountNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM account
        WHERE account_no = #{accountNo} AND deleted = 0
    </select>

    <select id="selectByType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM account
        WHERE account_type = #{accountType} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectActiveList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM account
        WHERE status = 1 AND deleted = 0
        ORDER BY account_type, create_time
    </select>

    <update id="updateBalance">
        UPDATE account 
        SET balance = balance + #{amount}
        WHERE id = #{id} AND deleted = 0
    </update>

</mapper>