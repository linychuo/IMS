<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.warehouse.mapper.WarehouseMapper">

    <resultMap id="BaseResultMap" type="com.ims.warehouse.entity.Warehouse">
        <id column="id" property="id"/>
        <result column="warehouse_code" property="code"/>
        <result column="warehouse_name" property="name"/>
        <result column="contact" property="contact"/>
        <result column="phone" property="phone"/>
        <result column="address" property="address"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_at" property="createTime"/>
        <result column="updated_at" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, warehouse_code, warehouse_name, contact, phone, address, status, remark, created_at, updated_at
    </sql>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM warehouse WHERE warehouse_code = #{code}
    </select>

    <select id="selectActiveList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM warehouse WHERE status = 0 ORDER BY warehouse_name ASC
    </select>

</mapper>