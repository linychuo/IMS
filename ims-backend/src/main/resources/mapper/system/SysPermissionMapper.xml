<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.system.mapper.SysPermissionMapper">

    <resultMap id="BaseResultMap" type="com.ims.system.entity.SysPermission">
        <id column="id" property="id"/>
        <result column="permission_code" property="permissionCode"/>
        <result column="permission_name" property="permissionName"/>
        <result column="permission_type" property="permissionType"/>
        <result column="parent_id" property="parentId"/>
        <result column="path" property="path"/>
        <result column="component" property="component"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="icon" property="icon"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, permission_code, permission_name, permission_type, parent_id,
        path, component, sort_order, icon, description, status,
        create_by, create_time, update_by, update_time, deleted
    </sql>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_permission
        WHERE permission_code = #{permissionCode} AND deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_permission
        WHERE deleted = 0
    </select>

    <select id="selectAllIncludingDeleted" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_permission
        WHERE deleted IN (0, 1)
    </select>

    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_permission
        WHERE deleted = 0 AND status = 1
    </select>

    <select id="selectByParentCode" resultMap="BaseResultMap">
        SELECT p.* FROM sys_permission p
        INNER JOIN sys_permission parent ON p.parent_id = parent.id
        WHERE parent.permission_code = #{parentCode} AND p.deleted = 0
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT p.<include refid="Base_Column_List"/>
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT DISTINCT p.id, p.permission_code, p.permission_name, p.permission_type, p.parent_id,
               p.path, p.component, p.sort_order, p.icon, p.description, p.status,
               p.create_by, p.create_time, p.update_by, p.update_time, p.deleted
        FROM sys_permission p
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId} AND p.deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_permission (
            permission_code, permission_name, permission_type, parent_id,
            path, component, sort_order, icon, description, status,
            create_time, update_time, deleted
        ) VALUES (
            #{permissionCode}, #{permissionName}, #{permissionType}, #{parentId},
            #{path}, #{component}, #{sortOrder}, #{icon}, #{description}, #{status},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 0
        )
    </insert>

    <update id="update">
        UPDATE sys_permission
        SET permission_name = #{permissionName},
            permission_type = #{permissionType},
            parent_id = #{parentId},
            path = #{path},
            component = #{component},
            sort_order = #{sortOrder},
            icon = #{icon},
            description = #{description},
            status = #{status},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE sys_permission
        SET deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="batchDelete">
        UPDATE sys_permission
        SET deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>