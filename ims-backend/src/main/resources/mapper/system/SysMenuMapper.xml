<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.system.mapper.SysMenuMapper">

    <resultMap id="BaseResultMap" type="com.ims.system.entity.SysMenu">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="path" property="path"/>
        <result column="parent_id" property="parentId"/>
        <result column="component" property="component"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, path, parent_id, component, sort_order, description, status, create_time, update_time, deleted
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_menu
        WHERE deleted = 0
        ORDER BY sort_order ASC
    </select>

    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_menu
        WHERE deleted = 0 AND status = 1
        ORDER BY sort_order ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_menu
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_menu (
            name, path, parent_id, component, sort_order, description, status, create_time, update_time, deleted
        ) VALUES (
            #{name}, #{path}, #{parentId}, #{component}, #{sortOrder}, #{description}, #{status},
            CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 0
        )
    </insert>

    <update id="update">
        UPDATE sys_menu
        SET name = #{name},
            path = #{path},
            parent_id = #{parentId},
            component = #{component},
            sort_order = #{sortOrder},
            description = #{description},
            status = #{status},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE sys_menu
        SET deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="batchDelete">
        UPDATE sys_menu
        SET deleted = 1, update_time = CURRENT_TIMESTAMP
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>