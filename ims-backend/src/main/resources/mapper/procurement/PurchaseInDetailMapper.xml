<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.procurement.mapper.PurchaseInDetailMapper">

    <resultMap id="BaseResultMap" type="com.ims.procurement.entity.PurchaseInDetail">
        <id column="id" property="id"/>
        <result column="in_id" property="inId"/>
        <result column="order_detail_id" property="orderDetailId"/>
        <result column="product_id" property="productId"/>
        <result column="product_code" property="productCode"/>
        <result column="product_name" property="productName"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="amount" property="amount"/>
        <result column="check_status" property="checkStatus"/>
        <result column="checked_qty" property="checkedQty"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, in_id, order_detail_id, product_id, product_code, product_name, spec, unit,
        quantity, price, amount, check_status, checked_qty, remark, create_time, update_time, create_by, update_by, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ims_purchase_in_detail
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByInId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ims_purchase_in_detail
        WHERE in_id = #{inId} AND deleted = 0
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.ims.procurement.entity.PurchaseInDetail">
        INSERT INTO ims_purchase_in_detail (
            in_id, order_detail_id, product_id, product_code, product_name, spec, unit,
            quantity, price, amount, remark, create_time, create_by, deleted
        ) VALUES (
            #{entity.inId}, #{entity.orderDetailId}, #{entity.productId}, #{entity.productCode},
            #{entity.productName}, #{entity.spec}, #{entity.unit}, #{entity.quantity},
            #{entity.price}, #{entity.amount}, #{entity.remark}, NOW(), #{entity.createBy}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.procurement.entity.PurchaseInDetail">
        UPDATE ims_purchase_in_detail
        SET order_detail_id = #{entity.orderDetailId},
            product_id = #{entity.productId},
            product_code = #{entity.productCode},
            product_name = #{entity.productName},
            spec = #{entity.spec},
            unit = #{entity.unit},
            quantity = #{entity.quantity},
            price = #{entity.price},
            amount = #{entity.amount},
            remark = #{entity.remark},
            update_time = NOW(),
            update_by = #{entity.updateBy}
        WHERE id = #{entity.id} AND deleted = 0
    </update>

    <delete id="deleteById">
        UPDATE ims_purchase_in_detail
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </delete>

    <delete id="deleteByInId">
        UPDATE ims_purchase_in_detail
        SET deleted = 1, update_time = NOW()
        WHERE in_id = #{inId}
    </delete>

    <update id="updateCheckStatus">
        UPDATE ims_purchase_in_detail
        SET check_status = #{checkStatus},
            checked_qty = #{checkedQty},
            update_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="batchUpdateCheckStatus">
        UPDATE ims_purchase_in_detail
        SET check_status = #{checkStatus},
            checked_qty = #{checkedQty},
            update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
    </update>

</mapper>