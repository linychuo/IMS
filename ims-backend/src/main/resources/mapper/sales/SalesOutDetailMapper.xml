<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.sales.mapper.SalesOutDetailMapper">

    <resultMap id="BaseResultMap" type="com.ims.sales.entity.SalesOutDetail">
        <id column="id" property="id"/>
        <result column="out_id" property="outId"/>
        <result column="order_detail_id" property="orderDetailId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="amount" property="amount"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="baseColumns">
        id, out_id, order_detail_id, product_id, product_name, spec, unit, quantity, price, amount, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sales_out_detail
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByOutId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sales_out_detail
        WHERE out_id = #{outId} AND deleted = 0
    </select>

    <select id="selectByOutNo" resultMap="BaseResultMap">
        SELECT d.* FROM sales_out_detail d
        JOIN sales_out o ON d.out_id = o.id
        WHERE o.out_no = #{outNo} AND d.deleted = 0
    </select>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO sales_out_detail (
            id, out_id, order_detail_id, product_id, product_name, spec, unit, quantity, price, amount, deleted
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.outId}, #{item.orderDetailId}, #{item.productId}, #{item.productName}, #{item.spec}, #{item.unit}, #{item.quantity}, #{item.price}, #{item.amount}, 0)
        </foreach>
    </insert>

    <delete id="deleteByOutId">
        UPDATE sales_out_detail SET deleted = 1 WHERE out_id = #{outId}
    </delete>

</mapper>