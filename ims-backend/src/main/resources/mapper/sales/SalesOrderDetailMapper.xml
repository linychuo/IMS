<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.sales.mapper.SalesOrderDetailMapper">

    <resultMap id="BaseResultMap" type="com.ims.sales.entity.SalesOrderDetail">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="amount" property="amount"/>
        <result column="out_quantity" property="outQuantity"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="baseColumns">
        id, order_id, order_no, product_id, product_name, spec, unit, quantity, price, amount, out_quantity, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sales_order_detail
        WHERE id = #{id}
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sales_order_detail
        WHERE order_id = #{orderId}
    </select>

    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM sales_order_detail
        WHERE order_no = #{orderNo}
    </select>

    <insert id="insert" parameterType="com.ims.sales.entity.SalesOrderDetail">
        INSERT INTO sales_order_detail (
            id, order_id, order_no, product_id, product_name, spec, unit, quantity, price, amount, out_quantity, create_time, update_time
        ) VALUES (
            #{id}, #{orderId}, #{orderNo}, #{productId}, #{productName}, #{spec}, #{unit}, #{quantity}, #{price}, #{amount}, #{outQuantity}, #{createTime}, #{updateTime}
        )
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO sales_order_detail (
            id, order_id, order_no, product_id, product_name, spec, unit, quantity, price, amount, out_quantity, create_time, update_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (nextval('sys_user_seq'), #{item.orderId}, #{item.orderNo}, #{item.productId}, #{item.productName}, #{item.spec}, #{item.unit}, #{item.quantity}, #{item.price}, #{item.amount}, #{item.outQuantity}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
        </foreach>
    </insert>

    <update id="update" parameterType="com.ims.sales.entity.SalesOrderDetail">
        UPDATE sales_order_detail
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="price != null">price = #{price},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="outQuantity != null">out_quantity = #{outQuantity},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteByOrderId">
        DELETE FROM sales_order_detail WHERE order_id = #{orderId}
    </delete>

</mapper>