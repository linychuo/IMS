<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.finance.mapper.AccountTransactionMapper">
    
    <select id="selectByAccountId" resultType="com.ims.finance.entity.AccountTransaction">
        SELECT * FROM account_transaction 
        WHERE account_id = #{accountId}
        ORDER BY id DESC
    </select>
    
    <select id="selectByAccountIdPaged" resultType="com.ims.finance.entity.AccountTransaction">
        SELECT * FROM account_transaction 
        WHERE account_id = #{accountId}
        ORDER BY id DESC
        LIMIT #{offset}, #{pageSize}
    </select>
    
</mapper>