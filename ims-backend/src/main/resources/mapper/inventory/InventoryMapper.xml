<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.inventory.mapper.InventoryMapper">

    <resultMap id="BaseResultMap" type="com.ims.inventory.entity.Inventory">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="quantity" property="quantity"/>
        <result column="reserved_quantity" property="reservedQuantity"/>
        <result column="available_quantity" property="availableQuantity"/>
        <result column="batch_no" property="batchNo"/>
        <result column="production_date" property="productionDate"/>
        <result column="expire_date" property="expireDate"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, product_id, warehouse_id, quantity, reserved_quantity, available_quantity,
        batch_no, production_date, expire_date, unit_price, total_amount,
        created_by, created_at, updated_by, updated_at, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE deleted = 0
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE product_id = #{productId} AND deleted = 0
    </select>

    <select id="selectByWarehouseId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE warehouse_id = #{warehouseId} AND deleted = 0
    </select>

    <select id="selectByProductAndWarehouse" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE product_id = #{productId} AND warehouse_id = #{warehouseId} AND deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.ims.inventory.entity.Inventory">
        INSERT INTO inventory (
            id, product_id, warehouse_id, quantity, reserved_quantity, available_quantity,
            batch_no, production_date, expire_date, unit_price, total_amount,
            created_by, created_at, deleted
        ) VALUES (
            #{id}, #{productId}, #{warehouseId}, #{quantity}, #{reservedQuantity}, #{availableQuantity},
            #{batchNo}, #{productionDate}, #{expireDate}, #{unitPrice}, #{totalAmount},
            #{createdBy}, #{createdAt}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.inventory.entity.Inventory">
        UPDATE inventory
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="reservedQuantity != null">reserved_quantity = #{reservedQuantity},</if>
            <if test="availableQuantity != null">available_quantity = #{availableQuantity},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="updatedBy != null">updated_by = #{updatedBy},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE inventory SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>