<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.inventory.mapper.InventoryMapper">

    <resultMap id="BaseResultMap" type="com.ims.inventory.entity.Inventory">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="location_id" property="locationId"/>
        <result column="quantity" property="quantity"/>
        <result column="frozen_quantity" property="frozenQuantity"/>
        <result column="cost" property="cost"/>
        <result column="batch_no" property="batchNo"/>
        <result column="production_date" property="productionDate"/>
        <result column="expiry_date" property="expiryDate"/>
        <result column="product_name" property="productName"/>
        <result column="product_code" property="productCode"/>
        <result column="warehouse_name" property="warehouseName"/>
        <result column="location_name" property="locationName"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, product_id, warehouse_id, location_id, quantity, frozen_quantity, cost,
        batch_no, production_date, expiry_date, product_name, product_code,
        warehouse_name, location_name, create_time, update_time, deleted
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE deleted = 0
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM inventory WHERE deleted = 0
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE deleted = 0
        <if test="productId != null">
            AND product_id = #{productId}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE product_id = #{productId} AND deleted = 0
    </select>

    <select id="selectByWarehouseId" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE warehouse_id = #{warehouseId} AND deleted = 0
    </select>

    <select id="selectByProductAndWarehouse" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory
        WHERE product_id = #{productId} AND warehouse_id = #{warehouseId} AND deleted = 0
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.ims.inventory.entity.Inventory">
        INSERT INTO inventory (
            id, product_id, warehouse_id, location_id, quantity, frozen_quantity, cost,
            batch_no, production_date, expiry_date, product_name, product_code,
            warehouse_name, location_name, create_time, deleted
        ) VALUES (
            #{id}, #{productId}, #{warehouseId}, #{locationId}, #{quantity}, #{frozenQuantity}, #{cost},
            #{batchNo}, #{productionDate}, #{expiryDate}, #{productName}, #{productCode},
            #{warehouseName}, #{locationName}, #{createTime}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.inventory.entity.Inventory">
        UPDATE inventory
        <set>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="frozenQuantity != null">frozen_quantity = #{frozenQuantity},</if>
            <if test="cost != null">cost = #{cost},</if>
            <if test="batchNo != null">batch_no = #{batchNo},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE inventory SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>