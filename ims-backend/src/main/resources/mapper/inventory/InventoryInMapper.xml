<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.inventory.mapper.InventoryInMapper">

    <resultMap id="BaseResultMap" type="com.ims.inventory.entity.InventoryIn">
        <id column="id" property="id"/>
        <result column="in_no" property="inNo"/>
        <result column="in_type" property="inType"/>
        <result column="ref_id" property="refId"/>
        <result column="ref_no" property="refNo"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="supplier_id" property="supplierId"/>
        <result column="in_date" property="inDate"/>
        <result column="status" property="status"/>
        <result column="approver_id" property="approverId"/>
        <result column="approve_time" property="approveTime"/>
        <result column="in_user_id" property="inUserId"/>
        <result column="remark" property="remark"/>
        <result column="created_by" property="createdBy"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_by" property="updatedBy"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, in_no, in_type, order_id, order_no, supplier_id, supplier_name, in_date, warehouse_id,
        warehouse_name, in_by, in_by_name, status, total_amount, remark,
        audited_by, audited_at, create_time, update_time, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_in
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByInNo" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_in
        WHERE in_no = #{inNo} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_in
        WHERE deleted = 0
        <if test="inType != null">
            AND in_type = #{inType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="supplierId != null">
            AND supplier_id = #{supplierId}
        </if>
        ORDER BY created_at DESC
    </select>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_in
        WHERE deleted = 0
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="inType != null">
            AND in_type = #{inType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM inventory_in WHERE deleted = 0
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="inType != null">
            AND in_type = #{inType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <insert id="insert" parameterType="com.ims.inventory.entity.InventoryIn">
        INSERT INTO inventory_in (
            id, in_no, in_type, order_id, order_no, supplier_id, supplier_name, in_date, warehouse_id,
            warehouse_name, in_by, in_by_name, status, total_amount, remark,
            audited_by, audited_at, create_time, deleted
        ) VALUES (
            #{id}, #{inNo}, #{inType}, #{orderId}, #{orderNo}, #{supplierId}, #{supplierName}, #{inDate}, #{warehouseId},
            #{warehouseName}, #{inBy}, #{inByName}, #{status}, #{totalAmount}, #{remark},
            #{auditedBy}, #{auditedAt}, #{createTime}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.inventory.entity.InventoryIn">
        UPDATE inventory_in
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="auditedBy != null">audited_by = #{auditedBy},</if>
            <if test="auditedAt != null">audited_at = #{auditedAt},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE inventory_in SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>