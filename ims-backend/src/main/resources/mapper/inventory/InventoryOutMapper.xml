<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.inventory.mapper.InventoryOutMapper">

    <resultMap id="BaseResultMap" type="com.ims.inventory.entity.InventoryOut">
        <id column="id" property="id"/>
        <result column="out_no" property="outNo"/>
        <result column="out_type" property="outType"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="warehouse_id" property="warehouseId"/>
        <result column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
        <result column="out_date" property="outDate"/>
        <result column="warehouse_name" property="warehouseName"/>
        <result column="out_by" property="outBy"/>
        <result column="out_by_name" property="outByName"/>
        <result column="status" property="status"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="remark" property="remark"/>
        <result column="audited_by" property="auditedBy"/>
        <result column="audited_at" property="auditedAt"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="baseColumns">
        id, out_no, out_type, order_id, order_no, warehouse_id, customer_id, customer_name,
        out_date, warehouse_name, out_by, out_by_name, status, total_amount, remark,
        audited_by, audited_at, create_time, update_time, deleted
    </sql>

    <select id="selectPage" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_out
        WHERE deleted = 0
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="outType != null">
            AND out_type = #{outType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id DESC
        LIMIT #{pageSize} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM inventory_out WHERE deleted = 0
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="outType != null">
            AND out_type = #{outType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_out
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByOutNo" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_out
        WHERE out_no = #{outNo} AND deleted = 0
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="baseColumns"/>
        FROM inventory_out
        WHERE deleted = 0
        <if test="outType != null">
            AND out_type = #{outType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="warehouseId != null">
            AND warehouse_id = #{warehouseId}
        </if>
        <if test="customerId != null">
            AND customer_id = #{customerId}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.ims.inventory.entity.InventoryOut">
        INSERT INTO inventory_out (
            id, out_no, out_type, order_id, order_no, warehouse_id, customer_id, customer_name,
            out_date, warehouse_name, out_by, out_by_name, status, total_amount, remark,
            audited_by, audited_at, create_time, deleted
        ) VALUES (
            #{id}, #{outNo}, #{outType}, #{orderId}, #{orderNo}, #{warehouseId}, #{customerId}, #{customerName},
            #{outDate}, #{warehouseName}, #{outBy}, #{outByName}, #{status}, #{totalAmount}, #{remark},
            #{auditedBy}, #{auditedAt}, #{createTime}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.inventory.entity.InventoryOut">
        UPDATE inventory_out
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="auditedBy != null">audited_by = #{auditedBy},</if>
            <if test="auditedAt != null">audited_at = #{auditedAt},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE inventory_out SET deleted = 1 WHERE id = #{id}
    </update>

</mapper>