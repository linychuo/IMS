<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.product.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.ims.product.entity.Product">
        <id column="id" property="id"/>
        <result column="product_code" property="code"/>
        <result column="product_name" property="name"/>
        <result column="category_id" property="categoryId"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="barcode" property="barcode"/>
        <result column="purchase_price" property="purchasePrice"/>
        <result column="sale_price" property="salePrice"/>
        <result column="min_sale_price" property="minSalePrice"/>
        <result column="stock_warning" property="stockWarning"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="image_url" property="imageUrl"/>
        <result column="created_at" property="createTime"/>
        <result column="updated_at" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, product_code, product_name, category_id, spec, unit, barcode, purchase_price, sale_price, min_sale_price, stock_warning, status, remark, image_url, created_at, updated_at
    </sql>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE product_code = #{code}
    </select>

    <select id="selectByBarcode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE barcode = #{barcode} AND deleted = 0
    </select>

    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE category_id = #{categoryId} ORDER BY created_at DESC
    </select>

    <select id="selectLowStock" resultMap="BaseResultMap">
        SELECT p.* FROM product p
        LEFT JOIN (
            SELECT product_id, SUM(CASE WHEN type = 'IN' THEN quantity ELSE -quantity END) as stock
            FROM inventory_record
            GROUP BY product_id
        ) i ON p.id = i.product_id
        WHERE p.status = 1 AND (i.stock IS NULL OR i.stock &lt; p.stock_warning)
        ORDER BY i.stock ASC
    </select>

</mapper>