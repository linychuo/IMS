<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.procurement.mapper.PurchaseOrderDetailMapper">

    <resultMap id="BaseResultMap" type="com.ims.procurement.entity.PurchaseOrderDetail">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_code" property="productCode"/>
        <result column="product_name" property="productName"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="quantity" property="quantity"/>
        <result column="price" property="price"/>
        <result column="amount" property="amount"/>
        <result column="in_quantity" property="inQuantity"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, product_id, product_code, product_name, spec, unit,
        quantity, price, amount, in_quantity, remark, create_time, update_time, create_by, update_by, deleted
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ims_purchase_order_detail
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ims_purchase_order_detail
        WHERE order_id = #{orderId} AND deleted = 0
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.ims.procurement.entity.PurchaseOrderDetail">
        INSERT INTO ims_purchase_order_detail (
            order_id, product_id, product_code, product_name, spec, unit,
            quantity, price, amount, in_quantity, remark, create_time, create_by, deleted
        ) VALUES (
            #{entity.orderId}, #{entity.productId}, #{entity.productCode}, #{entity.productName},
            #{entity.spec}, #{entity.unit}, #{entity.quantity}, #{entity.price},
            #{entity.amount}, #{entity.inQuantity}, #{entity.remark}, NOW(), #{entity.createBy}, 0
        )
    </insert>

    <update id="update" parameterType="com.ims.procurement.entity.PurchaseOrderDetail">
        UPDATE ims_purchase_order_detail
        SET product_id = #{entity.productId},
            product_code = #{entity.productCode},
            product_name = #{entity.productName},
            spec = #{entity.spec},
            unit = #{entity.unit},
            quantity = #{entity.quantity},
            price = #{entity.price},
            amount = #{entity.amount},
            in_quantity = #{entity.inQuantity},
            remark = #{entity.remark},
            update_time = NOW(),
            update_by = #{entity.updateBy}
        WHERE id = #{entity.id} AND deleted = 0
    </update>

    <delete id="deleteById">
        UPDATE ims_purchase_order_detail
        SET deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </delete>

    <delete id="deleteByOrderId">
        UPDATE ims_purchase_order_detail
        SET deleted = 1, update_time = NOW()
        WHERE order_id = #{orderId}
    </delete>

</mapper>