<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.product.mapper.ProductMapper">

    <resultMap id="BaseResultMap" type="com.ims.product.entity.Product">
        <id column="id" property="id"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="category" property="category"/>
        <result column="spec" property="spec"/>
        <result column="unit" property="unit"/>
        <result column="purchase_price" property="purchasePrice"/>
        <result column="sale_price" property="salePrice"/>
        <result column="min_stock" property="minStock"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, code, name, category, spec, unit, purchase_price, sale_price, min_stock, status, remark, create_time, update_time
    </sql>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE code = #{code}
    </select>

    <select id="selectByCategory" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM product WHERE category = #{category} ORDER BY create_time DESC
    </select>

    <select id="selectLowStock" resultMap="BaseResultMap">
        SELECT p.* FROM product p
        LEFT JOIN (
            SELECT product_id, SUM(CASE WHEN type = 'IN' THEN quantity ELSE -quantity END) as stock
            FROM inventory_record
            GROUP BY product_id
        ) i ON p.id = i.product_id
        WHERE p.status = 1 AND (i.stock IS NULL OR i.stock &lt; p.min_stock)
        ORDER BY i.stock ASC
    </select>

</mapper>