<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ims.finance.mapper.FinanceInMapper">

    <resultMap id="BaseResultMap" type="com.ims.finance.entity.FinanceIn">
        <id column="id" property="id"/>
        <result column="in_no" property="inNo"/>
        <result column="order_id" property="orderId"/>
        <result column="customer_id" property="customerId"/>
        <result column="amount" property="amount"/>
        <result column="discount_amount" property="discountAmount"/>
        <result column="pay_method" property="payMethod"/>
        <result column="bank_account" property="bankAccount"/>
        <result column="bank_name" property="bankName"/>
        <result column="pay_date" property="payDate"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="creator_id" property="creatorId"/>
        <result column="auditor_id" property="auditorId"/>
        <result column="audit_time" property="auditTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, in_no, order_id, customer_id, amount, discount_amount, pay_method, bank_account, 
        bank_name, pay_date, status, remark, creator_id, auditor_id, audit_time, 
        create_time, update_time, deleted
    </sql>

    <select id="selectByInNo" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM finance_in
        WHERE in_no = #{inNo} AND deleted = 0
    </select>

    <select id="selectByCustomerId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM finance_in
        WHERE customer_id = #{customerId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM finance_in
        WHERE status = 1 AND deleted = 0
        ORDER BY create_time ASC
    </select>

    <update id="updateBalanceByAudit">
        UPDATE finance_in 
        SET status = 2, auditor_id = #{auditorId}, audit_time = NOW()
        WHERE id = #{id} AND status = 1
    </update>

</mapper>